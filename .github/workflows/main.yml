on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.2.0
      with:
        server: 192.169.149.112
        username: YP@quickwaterheaterrepair.com
        password: ${{ secrets.ftp_password }}
        server-dir: ./Quickwaterheaterrepair.com/wp-content/plugins/wn-products-builder/
